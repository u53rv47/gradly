/** @format */

import React from 'react';

const About = () => {
  return <div style={{
    minWidth: "500px",
    minHeight: "250px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    background: "lightblue"
  }}>About</div>;
};

export default About;
